import type {RoundaboutSettings} from './roundabout';
export const MAX_SLIP_CROSS_OFFSET=600;
export type Band={id:string;type:"shoulder"|"bike"|"motorcycle"|"buffer";width:number};
export type Direction='incoming'|'outgoing';
export type Section={width:number;walk:number;bands:Band[]};
export type Pocket={lanes:number;length:number;taper:number};
export type Pockets={left:Pocket;right:Pocket};
export const emptyPockets=():Pockets=>({left:{lanes:0,length:25,taper:15},right:{lanes:0,length:25,taper:15}});
export const sectionFor=(a:Arm,side:Direction):Section=>a[side==='incoming'?'incomingSection':'outgoingSection']??{width:a.width,walk:a.walk,bands:a.bands};
export const pocketsFor=(a:Arm,side:Direction):Pockets=>a[side==='incoming'?'incomingPockets':'outgoingPockets']??emptyPockets();
const validBands=(v:Band[])=>Array.isArray(v)&&v.length<=6&&v.every(b=>!!b&&typeof b.id==='string'&&['shoulder','bike','motorcycle','buffer'].includes(b.type)&&Number.isFinite(b.width)&&b.width>=.25&&b.width<=4.5);
const validSection=(s:Section)=>!!s&&Number.isFinite(s.width)&&s.width>=2.5&&s.width<=4.5&&Number.isFinite(s.walk)&&s.walk>=0&&s.walk<=5&&validBands(s.bands);
const validPockets=(p:Pockets)=>!!p&&['left','right'].every(k=>{const v=p[k as keyof Pockets];return !!v&&[0,1,2,3].includes(v.lanes)&&Number.isFinite(v.length)&&v.length>=5&&v.length<=100&&Number.isFinite(v.taper)&&v.taper>=5&&v.taper<=60;});
export type Roadside={trees:boolean;lights:boolean;spacing:number;offset:number;start:number;stagger:boolean;treeHeight:number;treeSize:number;lightHeight:number;lightSize:number;lightReach:number};
export const roadsideDefaults=():Roadside=>({trees:false,lights:false,spacing:15,offset:.5,start:10,stagger:false,treeHeight:5,treeSize:3.2,lightHeight:8,lightSize:.22,lightReach:3});
export function roadsideFor(d:Design,a:Arm,side:'incoming'|'outgoing'):Roadside{return a.roadside?.[side]??{...roadsideDefaults(),trees:d.trees,lights:d.lights,spacing:d.objectSpacing,offset:d.objectOffset,start:d.objectStart,stagger:d.stagger};}
function validRoadside(v:Roadside){return !!v&&['trees','lights','stagger'].every(k=>typeof v[k as keyof Roadside]==='boolean')&&Object.entries({spacing:[5,50],offset:[0,5],start:[0,40],treeHeight:[2,15],treeSize:[1,8],lightHeight:[3,15],lightSize:[.1,.6],lightReach:[1,6]}).every(([k,[lo,hi]])=>{const n=v[k as keyof Roadside];return typeof n==='number'&&Number.isFinite(n)&&n>=lo&&n<=hi;});}
export type Arm={corridorMode?:'preserve'|'widen';incomingSection?:Section;outgoingSection?:Section;incomingPockets?:Pockets;outgoingPockets?:Pockets;outgoingDividerMode?:'solid'|'dashed';outgoingSolidLength?:number;dividerMode?:'solid'|'dashed';solidLength?:number;roadside?:{incoming:Roadside;outgoing:Roadside};angle:number;length:number;bands:Band[];stopOffset:number;slipCrossing:boolean;slipCrossOffset:number;medianOffset:number;crossOffset:number;slipWidth:number;slipRadius:number;slip:boolean;name:string;incoming:number;outgoing:number;width:number;median:number;crossing:boolean;signal:boolean;stop:boolean;walk:number;arrows:string[]};
export type Design={roundabout?:RoundaboutSettings;schemaVersion:number;trees:boolean;lights:boolean;objectSpacing:number;objectOffset:number;objectStart:number;stagger:boolean;showNames:boolean;showScale:boolean;showArrows:boolean;enabled:boolean[];corner:number;circulation:number;type:string;arms:Arm[];rotation:number;radius:number;ring:number;title:string};
export const names=['ตะวันออก','ใต้','ตะวันตก','เหนือ'];
export const options:Record<string,string>={straight:'ตรง',left:'ซ้าย',right:'ขวา',sl:'ตรง + ซ้าย',sr:'ตรง + ขวา',lr:'ซ้าย + ขวา',all:'ตรง + ซ้าย + ขวา',ru:'ขวา + กลับรถ',uturn:'กลับรถ',su:'ตรง + กลับรถ',none:'ไม่มีลูกศร'};
export const initial=():Design=>({schemaVersion:3,trees:false,lights:false,objectSpacing:15,objectOffset:.5,objectStart:10,stagger:false,showNames:true,showScale:true,showArrows:true,enabled:[true,true,true,true],corner:10,circulation:7,type:'junction',title:'แบบทางแยกแนวคิด',rotation:0,radius:15,ring:2,arms:names.map((name,i)=>({corridorMode:'preserve',dividerMode:"solid",solidLength:30,angle:i*90,length:92,bands:[],stopOffset:2,slipCrossing:false,slipCrossOffset:10,medianOffset:9,crossOffset:4,slipWidth:4,slipRadius:32,slip:false,name,incoming:2,outgoing:2,width:3.25,median:2,crossing:true,signal:true,stop:true,walk:2,arrows:['sr','sl','straight','straight']}))});
export function valid(d:unknown):d is Design{if(!d||typeof d!=='object')return false;const v=d as Design;return v.schemaVersion===3&&(v.roundabout===undefined||Object.entries({apron:[0,4],entryRadius:[6,35],exitRadius:[8,45],splitterLength:[12,40],splitterWidth:[1,6],yieldOffset:[.3,3]}).every(([k,[lo,hi]])=>{const n=v.roundabout![k as keyof RoundaboutSettings];return Number.isFinite(n)&&n>=lo&&n<=hi;}))&&['trees','lights','stagger'].every(k=>typeof v[k as keyof Design]==='boolean')&&Number.isFinite(v.objectSpacing)&&v.objectSpacing>=5&&v.objectSpacing<=50&&Number.isFinite(v.objectOffset)&&v.objectOffset>=0&&v.objectOffset<=5&&Number.isFinite(v.objectStart)&&v.objectStart>=0&&v.objectStart<=40&&['showNames','showScale','showArrows'].every(k=>typeof v[k as keyof Design]==='boolean')&&['junction','roundabout'].includes(v.type)&&typeof v.title==='string'&&v.title.length<=100&&Number.isFinite(v.rotation)&&v.rotation>=0&&v.rotation<=360&&Number.isFinite(v.radius)&&v.radius>=10&&v.radius<=30&&[1,2,3].includes(v.ring)&&Array.isArray(v.arms)&&v.arms.length===4&&Array.isArray(v.enabled)&&v.enabled.length===4&&v.enabled.every(x=>typeof x==='boolean')&&v.enabled.filter(Boolean).length>=3&&Number.isFinite(v.corner)&&v.corner>=4&&v.corner<=20&&Number.isFinite(v.circulation)&&v.circulation>=3&&v.circulation<=22&&v.arms.every(a=>(a.corridorMode===undefined||['preserve','widen'].includes(a.corridorMode))&&(a.incomingSection===undefined||validSection(a.incomingSection))&&(a.outgoingSection===undefined||validSection(a.outgoingSection))&&(a.incomingPockets===undefined||validPockets(a.incomingPockets))&&(a.outgoingPockets===undefined||validPockets(a.outgoingPockets))&&(a.outgoingDividerMode===undefined||["solid","dashed"].includes(a.outgoingDividerMode))&&(a.outgoingSolidLength===undefined||(Number.isFinite(a.outgoingSolidLength)&&a.outgoingSolidLength>=1&&a.outgoingSolidLength<=100))&&(a.dividerMode===undefined||["solid","dashed"].includes(a.dividerMode))&&(a.solidLength===undefined||(Number.isFinite(a.solidLength)&&a.solidLength>=1&&a.solidLength<=100))&&(a.roadside===undefined||(validRoadside(a.roadside.incoming)&&validRoadside(a.roadside.outgoing)))&&Number.isFinite(a.angle)&&a.angle>=0&&a.angle<360&&Number.isFinite(a.length)&&a.length>=60&&a.length<=180&&Array.isArray(a.bands)&&a.bands.length<=6&&a.bands.every(b=>typeof b.id==='string'&&['shoulder','bike','motorcycle','buffer'].includes(b.type)&&Number.isFinite(b.width)&&b.width>=.25&&b.width<=4.5)&&Number.isFinite(a.stopOffset)&&a.stopOffset>=0&&a.stopOffset<=35&&typeof a.slipCrossing==='boolean'&&Number.isFinite(a.slipCrossOffset)&&a.slipCrossOffset>=2&&a.slipCrossOffset<=MAX_SLIP_CROSS_OFFSET&&Number.isFinite(a.medianOffset)&&a.medianOffset>=0&&a.medianOffset<=35&&Number.isFinite(a.crossOffset)&&a.crossOffset>=0&&a.crossOffset<=35&&Number.isFinite(a.slipWidth)&&a.slipWidth>=3&&a.slipWidth<=6&&Number.isFinite(a.slipRadius)&&a.slipRadius>=20&&a.slipRadius<=50&&typeof a.slip==='boolean'&&typeof a.name==='string'&&a.name.length<=40&&[0,1,2,3,4].includes(a.incoming)&&[0,1,2,3,4].includes(a.outgoing)&&a.incoming+a.outgoing>0&&a.width>=2.5&&a.width<=4.5&&Number.isFinite(a.width)&&a.median>=0&&a.median<=8&&Number.isFinite(a.median)&&a.walk>=0&&a.walk<=5&&Number.isFinite(a.walk)&&['crossing','signal','stop'].every(k=>typeof a[k as keyof Arm]==='boolean')&&Array.isArray(a.arrows)&&a.arrows.length===4&&a.arrows.every(c=>Object.hasOwn(options,c)));}

export function migrate(raw:any):Design{
 if(raw?.schemaVersion>3)throw Error('Unsupported future schema');
 if(!raw||typeof raw.type!=='string'||!Array.isArray(raw.arms))throw Error('Invalid design');
 const defaults=initial(),r=raw.type.startsWith('round');
 const d={...defaults,...raw,schemaVersion:3,type:r?'roundabout':'junction',enabled:raw.enabled??[0,1,2,3].map(i=>!['three','round3'].includes(raw.type)||i!==(raw.omitted??3)),circulation:raw.circulation??raw.ring*3.5,arms:raw.arms.map((a:Arm,i:number)=>({...defaults.arms[i],...a,corridorMode:a.corridorMode??(raw.schemaVersion===3?'preserve':'widen'),angle:a.angle??i*90,length:a.length??92,bands:a.bands??[],medianOffset:a.medianOffset??(r?0:9),crossOffset:a.crossOffset??(r?14:4)}))};
 if(!valid(d))throw Error('Invalid design');return d;
}
