import {useState} from 'react';
import {Dialog,DialogContent,DialogHeader,DialogTitle,DialogDescription} from '@/components/ui/dialog';
export type Command={label:string;run:()=>void};
export function ActionSearch({open,onOpenChange,actions}:{open:boolean;onOpenChange:(v:boolean)=>void;actions:Command[]}){const [q,setQ]=useState('');const filtered=actions.filter(a=>a.label.toLowerCase().includes(q.toLowerCase()));return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="action-search"><DialogHeader><DialogTitle>ค้นหาคำสั่ง</DialogTitle><DialogDescription>เลือกคำสั่งสำหรับแบบปัจจุบัน · Ctrl / Cmd + K</DialogDescription></DialogHeader><input autoFocus aria-label="ค้นหาคำสั่ง" placeholder="Turn lane, ต้นไม้, ส่งออก…" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&filtered[0]){filtered[0].run();onOpenChange(false);}}}/><div>{filtered.map(a=><button key={a.label} onClick={()=>{a.run();onOpenChange(false);}}>{a.label}</button>)}{!filtered.length&&<p>ไม่พบคำสั่ง</p>}</div></DialogContent></Dialog>;}
